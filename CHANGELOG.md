# VRCX-Luo 更新日志

## 2026.09.13

### VR 与启动体验

- 正式 VR 模式不再显示手背指针的调试选择原点，避免调试标记进入用户界面。
- 保留 `?wrist-pointer-test=1` 桌面测试入口，便于后续继续验证手背指针位置和点击行为。
- 保留手柄扳机方向、全手背坐标范围、页面切换和点击边沿修正。
- 修复开发模式入口、Vue 预构建和首次启动相关的空白页面问题。

### 好友、通知与托盘

- 首次启动先恢复本地好友缓存，右侧好友栏无需等待完整 API 同步即可显示。
- 好友日志、好友关系变化和解除好友事件可投影到托盘通知，并与 App 内红点状态同步。
- 修正 Windows 任务栏图标初始化、桌面通知开关状态文案和通知测试行为。
- 离线好友按最近离线时间优先排序。

### 房间与关系数据

- 房间进出记录支持方向和好友身份筛选、筛选记忆，并使用虚拟列表降低大数据量切换卡顿。
- 双人关系页面增加共同房间访问次数和共同地图排行榜悬浮窗。
- 群组页面、群组房间、公告、活动、相册和成员管理继续沿用统一的卡片布局与操作反馈。

### 搜索与界面

- 左上角搜索支持非连续模糊匹配、中文拼音首字母和全拼匹配。
- 完善戳一戳自定义贴纸的选中反馈、卡片填充和键盘操作。
- 对话框支持从来源卡片展开/收回的非线性动画；动画偏好保持可配置。
- 持续收敛 CEF/Electron 双宿主、通知、窗口和状态管理边界，保留现有公共接口。

### 稳定性与验证

- 版本号统一为 `2026.09.13`。
- 生产前端构建通过（4430 个模块）。
- 手背指针定向回归测试通过（19/19）。
- 本版本由 GitHub Actions 构建 Windows、Linux 和 macOS 发布产物。

### 架构重构与工程治理

- 完成 UpdateLoop 调度器与独立任务模块拆分，保留原有启动/停止入口、任务周期和 fake-clock 测试。
- 完成 CEF/Electron capability adapter、可信渲染来源校验和 .NET bridge 参数 schema，降低跨宿主调用风险。
- 完成 coordinator 的 DOM、Toast、Router、通知副作用 adapter 化，Store 继续提供兼容入口。
- 完成 Notification Store 的视图/实体 projection、偏好持久化和已读队列拆分。
- 完成 Electron 窗口、托盘、桌面通知、IPC 注册和生命周期的 composition-root seam。
- 完成 Query 缓存 side effect、scope key 和 resource registry 收敛，保留 API/认证层原有行为。
- 完成 Group、Favorite、User 纯决策与 projection 模块拆分，减少上帝模块职责。
- 完成 GameLog parser、数据库只读 projection、查询参数、会话过滤和 now-playing ticker 拆分。
- 完成前端 Appearance、body drop guard、对话框面包屑和托盘通知宿主 adapter 重构；前端重构达到停止线。
- 建立版本来源一致性、Shared/Localization 依赖方向、语言包契约、依赖版本矩阵和生成文件治理门禁。
- 多账户数据隔离相关实现继续按计划暂缓，不在本版本扩大范围。
